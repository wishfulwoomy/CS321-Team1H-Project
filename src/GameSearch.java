import java.util.ArrayList;

// NOTES FOR LATER: user needs to be able to search by query or filter OR BOTH
// current layout doesn't support both. fix that

public class GameSearch {
    /**
     * @param query The text query, taken from user input and used to search GameCollection.
     * @return A filtered list of games.
     */
    public ArrayList<Game> searchByQuery(String query) {
        ArrayList<Game> SearchResults = new ArrayList<Game>();
        // TO-DO: IMPLEMENT SEARCH
        return SearchResults;
    }
    public ArrayList<Game> searchByFilter() {
        ArrayList<Game> SearchResults = new ArrayList<Game>();
        // TO-DO: IMPLEMENT SEARCH
        return SearchResults;
    }
}
